To render the document on Linux:

1. Install "pandoc" (a new enough version that `pandoc --citeproc` works --
   this may require downloading a recent binary or source package,
   even on a fairly recent Ubuntu or WSL)
2. Install the Python "panflute" package
3. Run make
